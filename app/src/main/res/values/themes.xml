<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DoctorList" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="colorButtonNormal">#0000FF</item>
        <item name="colorControlHighlight">#673ab7</item>
        <!-- Customize your theme here. -->
    </style>


    <style name="Button1" parent="@android:style/Widget.Button">
        <item name="background">@drawable/button</item>
        <item name="android:textColor">@color/white</item>
        <item name="elevation">2dp</item>
        <item name="pressedTranslationZ">4dp</item>
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
    </style>


    <style name="Button2" parent="@android:style/Widget.Button">
        <item name="background">@drawable/button</item>
        <item name="android:textColor">@color/black</item>
        <item name="elevation">2dp</item>
        <item name="pressedTranslationZ">4dp</item>
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
    </style>
</resources>